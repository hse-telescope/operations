port: 8080

db:
  schema: "postgres"
  user: "user"
  password: "password"
  ip: "auth-db"
  port: 5432
  database: "users"
  ssl: "disable"
  migrations_path: "github://hse-telescope/auth/migrations"

clients:
  example:
    url: http://localhost:8080

logger:
  mode: info
